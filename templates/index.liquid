<div class="hero">
  <div class="container">
    <h1>🥤 Fresh Cold-Pressed Juices</h1>
    <p>100% natural, made daily with love in Medellín, Colombia</p>
    <a href="/collections/all" class="btn">See Our Menu</a>
  </div>
</div>

<div class="container">
  <section class="featured-products">
    <h2>Popular Drinks</h2>
    <div class="products-grid">
      {% for product in collections.featured.products limit: 3 %}
        <div class="product-card">
          {% if product.featured_image %}
            <img src="{{ product.featured_image | img_url: '300x300' }}" alt="{{ product.title }}">
          {% endif %}
          <h3>{{ product.title }}</h3>
          <p>{{ product.price | money }}</p>
          <button class="add-to-cart btn" data-product-id="{{ product.selected_or_first_available_variant.id }}">
            Add to Cart
          </button>
        </div>
      {% endfor %}
    </div>
  </section>

  <section class="about">
    <h2>Why Choose Us?</h2>
    <div class="features">
      <div class="feature">
        <h3>🌱 100% Natural</h3>
        <p>No artificial preservatives or additives</p>
      </div>
      <div class="feature">
        <h3>🥕 Fresh Daily</h3>
        <p>Made fresh every morning with local ingredients</p>
      </div>
      <div class="feature">
        <h3>🚚 Fast Delivery</h3>
        <p>Quick delivery throughout Medellín</p>
      </div>
    </div>
  </section>
</div>

<style>
.products-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
  margin: 2rem 0;
}

.product-card {
  background: white;
  border-radius: 10px;
  padding: 1rem;
  box-shadow: 0 4px 20px rgba(0,0,0,0.1);
  text-align: center;
}

.product-card img {
  width: 100%;
  height: 200px;
  object-fit: cover;
  border-radius: 8px;
  margin-bottom: 1rem;
}

.features {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
  margin: 2rem 0;
}

.feature {
  text-align: center;
  padding: 2rem;
  background: #f8f9fa;
  border-radius: 10px;
}

.feature h3 {
  font-size: 1.5rem;
  margin-bottom: 1rem;
}

@media (max-width: 768px) {
  .hero h1 {
    font-size: 2rem;
  }
  
  .products-grid,
  .features {
    grid-template-columns: 1fr;
  }
}
</style>
